import React from "react";

const Form = () => {
    return (
        <form>
            <label htmlFor="name">Add member</label>
            <input id="fname"
            type="text"
            name="textfield"
            placeholder="First name"
            />
        </form>
    )
};

export default Form;